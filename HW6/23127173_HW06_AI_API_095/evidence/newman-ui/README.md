# Newman UI evidence

`newman-baseline-terminal-20260901.png` là ảnh chụp cửa sổ Terminal từ lần chạy baseline thật trên `127.0.0.1:3000`. Ảnh hiển thị collection-level log `X-Student-Id: 23127173` và summary 20 requests, 21 assertions, 0 failures.
