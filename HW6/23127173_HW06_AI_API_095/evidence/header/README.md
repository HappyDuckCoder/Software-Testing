# Header evidence

The student-provided Postman headers capture verifies a live request to
`http://127.0.0.1:3000/api/checkout` with `200 OK` and
`X-Student-Id: 23127173`. Its source copy belongs in
`../postman-ui/postman-request-headers-student-id-20260901.png` after the
Bearer token is redacted. This is stronger execution evidence than a script
editor or collection configuration view.
