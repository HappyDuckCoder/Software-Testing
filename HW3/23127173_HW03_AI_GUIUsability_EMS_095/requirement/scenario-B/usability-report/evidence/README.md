# Bằng chứng phiên Task 2

`participants/` chứa 5 ảnh do sinh viên cung cấp, tương ứng P1–P5 trong `../usability-report.md`. Pilot-01 là người ngoài lớp, tách biệt P1–P5; không có ảnh/video pilot vì phiên này chỉ dùng kiểm tra task/luồng theo quyết định sinh viên.

Không đặt ảnh kiểm thử GUI/compatibility tại đây. Ảnh finding vẫn ở `../screenshots/`; ảnh Task 3 vẫn ở `../../compatibility-matrix/screenshots/`.
