# Bằng chứng phiên Task 2

`participants/` chứa 5 ảnh do sinh viên cung cấp, tương ứng P1–P5 trong `../usability-report.md`. Pilot dùng cùng P1 theo quyết định đã khai báo, nên không có ảnh Pilot riêng.

Không đặt ảnh kiểm thử GUI/compatibility tại đây. Ảnh finding vẫn ở `../screenshots/`; ảnh Task 3 vẫn ở `../../compatibility-matrix/screenshots/`.
