# Test plans

Đặt ba test plan đã chạy và được human review tại đây. Dùng tên `{StudentID}_{ScenarioType}_{YYYYMMDD}` với `ScenarioType` là `Load`, `Stress`, `Spike`. Không thêm file `.jmx`/`.js` trống làm bằng chứng thay cho plan thật.
